[workspace]
resolver = "3"
members = [
    "kernel",
    "bootloader",
]

[workspace.metadata.bootimage]
kernel = "kernel"
kernel-path = "target/x86_64-unknown-none/debug/kernel"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
